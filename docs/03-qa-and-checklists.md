# QA/체크리스트 통합 문서

아래 내용은 기존 문서의 본문을 파일 단위로 병합한 것입니다.

---

## 원본: manual-qa-checklist-2026-04-08.md

# 수동 QA 체크리스트 (2026-04-08)

## 범위
- `delivery/history` 성격 화면
- `sales/material` 영업자료실
- `master/performance-plan` 성과 계획 관리
- `delivery/plan` 스펙 추가/취소 탭
- `delivery/demand-forecast` 수요 예측
- `partner/notice` 공지/자료실
- `profit/*` 손익분석 상세

## 공통 사전 점검
- [ ] `npm run smoke:pages` 실행 결과가 모두 `OK` 이다.
- [ ] 콘솔 에러/경고가 반복적으로 발생하지 않는다.
- [ ] 한글 라벨/버튼/테이블 헤더가 깨지지 않는다.
- [ ] 버튼 클릭 시 레이아웃이 흔들리거나 요소가 겹치지 않는다.
- [ ] 1366x768, 1920x1080 기준으로 주요 영역이 정상 노출된다.

## 1) 영업자료실 (`/sales/material`)
- [ ] 조회 입력 후 `조회` 동작이 정상이다.
- [ ] 목록 헤더(`제목/유형/등록일/첨부`) 정렬과 폭이 정상이다.
- [ ] 다운로드 버튼 텍스트가 세로 깨짐 없이 가로 정렬로 보인다.
- [ ] 긴 제목 데이터에서도 행 높이/줄바꿈이 깨지지 않는다.
- [ ] `등록` 버튼 클릭 시 등록 플로우가 정상 진입한다.

## 2) 성과 계획 관리 (`/master/performance-plan`)
- [ ] 우측 상단 `저장` 버튼 위치가 조회 버튼 라인과 시각적으로 맞다.
- [ ] 탭(`팀 계획/개인 계획/일괄 업로드`) 전환 시 레이아웃 깨짐이 없다.
- [ ] 테이블 본문 텍스트가 세로 출력되지 않고 가로로 정상 표시된다.
- [ ] 상세 보기 진입 방식이 팝업으로 정상 동작한다.
- [ ] 페이지 우측 여백이 과도하지 않고 컨텐츠 폭이 자연스럽다.

## 3) 납품 계획 관리 (`/delivery/plan`)
- [ ] `스펙 추가` 탭 UI(필터/그리드/버튼)가 기존 스타일과 동일하다.
- [ ] `스펙 취소` 탭 UI(필터/그리드/버튼)가 기존 스타일과 동일하다.
- [ ] 탭 전환 시 데이터/버튼이 사라지지 않는다.
- [ ] 한글 텍스트 깨짐 없이 표시된다.

## 4) 수요 예측 (`/delivery/demand-forecast`)
- [ ] 예측수량을 기존값과 다르게 입력하면 `사유` 입력이 활성화된다.
- [ ] 기존값과 동일하게 되돌리면 `사유` 입력이 비활성화된다.
- [ ] 수정이력 버튼/영역이 노출되고 데이터가 보인다.
- [ ] 저장 후 재조회 시 변경값과 사유가 유지된다(목업 기준 확인).

## 5) 공지/자료실 (`/partner/notice`)
- [ ] 목록/상세 스타일이 프로젝트 공통 톤과 일치한다.
- [ ] 긴 본문 데이터에서 상세 내용 영역 스크롤이 정상 동작한다.
- [ ] 등록 화면 진입/입력/저장 동선이 정상이다.
- [ ] 노출기간/고정여부 등 고도화 필드가 있으면 저장/반영이 정상이다.

## 6) 손익분석 상세 (`/profit/...`)
- [ ] 목록에서 상세 클릭 시 상세 페이지(또는 모달)로 정상 진입한다.
- [ ] 상세 데이터(요약/테이블/차트)가 빈 화면 없이 노출된다.
- [ ] 목업 데이터가 없어도 안내 문구 또는 기본 상태가 정상 출력된다.

## 7) 배포 전 최종 회귀
- [ ] 주요 페이지 이동(사이드 메뉴) 시 404/빈 화면이 없다.
- [ ] 다크/라이트 변형이 있다면 기본 테마에서 가독성 문제가 없다.
- [ ] 저장/조회/초기화 핵심 버튼의 disabled 조건이 의도대로 동작한다.

## 실행 순서 권장
1. `sales/material` → 2. `delivery/plan` → 3. `delivery/demand-forecast`
2. `partner/notice` → 5. `master/performance-plan` → 6. `profit/*`

## 발견 이슈 기록 템플릿
```
[페이지]
- 재현 경로:
- 기대 결과:
- 실제 결과:
- 스크린샷:
- 심각도: (높음/중간/낮음)
```

---

## 원본: manual-qa-execution-2026-04-08.md

# 수동 QA 실행 리포트 (2026-04-08)

## 실행 범위
- `/sales/material`
- `/master/performance-plan`
- `/delivery/plan`
- `/delivery/demand-forecast`
- `/partner/notice`
- `/profit/*`

## 자동 검증 결과
- `npm run smoke:pages` : 통과
- `npm run lint` : 통과
- `npm run build` : 통과
- `npm run check:encoding` : 통과 (`364 files scanned`)

## 코드 기반 사전 점검 결과
- `sales/material`
  - 툴바 정렬, 검색창/등록 버튼 배치 보정 완료
  - 다운로드 버튼 세로 표시 방지(`writing-mode`, `nowrap`) 적용
- `master/performance-plan`
  - 상단 액션/필터/테이블 간격 및 기준선 정렬 보정 완료
  - 월 입력 폭/테이블 최소폭 보정 완료
- `delivery/demand-forecast`
  - 한글 깨짐 복구 완료
  - 예측수량 변경 시 사유 활성화 로직 정상 유지 확인
  - 수정 이력 모달/저장 메시지 정상화
- `delivery/plan` (스펙 추가/취소)
  - 한글 깨짐 복구 완료
  - 선택/반영/복원 플로우 문구 및 동작 정상화
- `profit/*`
  - 라우터 경로(`/profit/:id`, `/profit/:id/edit`) 및 목록-상세 이동 코드 연결 확인

## 수동 화면 확인 상태
- 현재 리포트는 코드/정적 검증 기준 결과입니다.
- 브라우저에서 실제 화면 픽셀 확인은 운영자 수동 확인이 필요합니다.
- 권장 우선 확인:
  1. `/sales/material` 첨부 버튼 가독성
  2. `/master/performance-plan` 테이블 가로 스크롤 및 버튼 정렬
  3. `/delivery/plan` 탭 전환 시 UI 유지 여부

## 결론
- 배포 차단 이슈 없음
- UI 미세 확인(수동 화면 확인)만 완료하면 마감 가능

---

## 원본: manual-qa-team-tracker-2026-04-08.md

# 수동 QA 팀 트래커 (2026-04-08)

## 사용 방법
- 담당자 배정 후 `상태`를 `대기/진행중/완료/이슈`로 변경합니다.
- 이슈 발생 시 `이슈 메모`에 재현 경로와 증상을 기록합니다.

## 공통 검증
| 구분 | 체크 항목 | 담당자 | 상태 | 완료일 | 이슈 메모 |
|---|---|---|---|---|---|
| 공통 | `npm run smoke:pages` 통과 | Codex | 완료 | 2026-04-08 | 통과 |
| 공통 | `npm run lint` 통과 | Codex | 완료 | 2026-04-08 | 통과 |
| 공통 | `npm run build` 통과 | Codex | 완료 | 2026-04-08 | 통과 |
| 공통 | `npm run check:encoding` 통과 | Codex | 완료 | 2026-04-08 | 364 files OK |
| 공통 | 콘솔 에러/경고 반복 없음 | QA 담당 | 대기 |  | 브라우저 수동 확인 필요 |
| 공통 | 한글 깨짐 없음 | QA 담당 | 대기 |  | 브라우저 수동 확인 필요 |
| 공통 | 1366/1920 해상도 레이아웃 정상 | QA 담당 | 대기 |  | 브라우저 수동 확인 필요 |

## 페이지별 검증
| 페이지 | 체크 항목 | 담당자 | 상태 | 완료일 | 이슈 메모 |
|---|---|---|---|---|---|
| `/sales/material` | 목록 헤더/행 정렬 정상 | QA 담당 | 대기 |  |  |
| `/sales/material` | 첨부 다운로드 버튼 가독성 정상 | QA 담당 | 대기 |  |  |
| `/master/performance-plan` | 저장 버튼/조회 버튼 정렬 정상 | QA 담당 | 대기 |  |  |
| `/master/performance-plan` | 표 텍스트 세로 깨짐 없음 | QA 담당 | 대기 |  |  |
| `/master/performance-plan` | 상세 팝업 동작 정상 | QA 담당 | 대기 |  |  |
| `/delivery/plan` | 스펙 추가 탭 UI/기능 정상 | Codex | 완료(코드) | 2026-04-08 | 탭 연결/컴포넌트 로딩 확인 |
| `/delivery/plan` | 스펙 취소 탭 UI/기능 정상 | Codex | 완료(코드) | 2026-04-08 | 탭 연결/컴포넌트 로딩 확인 |
| `/delivery/demand-forecast` | 예측수량 변경 시 사유 활성화 | Codex | 완료(코드) | 2026-04-08 | disabled 조건 로직 확인 |
| `/delivery/demand-forecast` | 수정이력 보기 정상 | Codex | 완료(코드) | 2026-04-08 | history modal/render 확인 |
| `/partner/notice` | 목록/상세/등록 스타일 정상 | QA 담당 | 대기 |  |  |
| `/profit/*` | 상세 진입/노출 정상 | Codex | 완료(코드) | 2026-04-08 | router 경로/상세 페이지 연결 확인 |

## 최종 승인
| 항목 | 담당자 | 상태 | 완료일 | 비고 |
|---|---|---|---|---|
| QA 종합 확인 | QA 리드 | 대기 |  |  |
| 배포 승인 | PO/PM | 대기 |  |  |

---

## 원본: final-visual-checklist-3pages-2026-04-08.md

# 최종 시각 점검표 (3페이지) - 2026-04-08

## 대상
- `/sales/material`
- `/master/performance-plan`
- `/partner/notice`

## 점검 방식
- 해상도 2종: `1366x768`, `1920x1080`
- 각 페이지별로 `전체 화면 1장 + 핵심 영역 1장` 스크린샷 저장
- 결과 상태: `PASS / FAIL`

## 1) `/sales/material` 영업자료실
### 체크 항목
- [ ] 페이지 타이틀/설명 위치가 다른 페이지와 톤이 동일하다.
- [ ] 검색창과 `등록` 버튼이 한 줄 정렬되고 높이가 맞다.
- [ ] 테이블 헤더(`제목/유형/등록일/첨부`) 정렬이 맞다.
- [ ] `다운로드` 버튼 텍스트가 세로가 아닌 가로로 보인다.
- [ ] 첨부 없음(`없음`) 표기 위치가 행 중앙 정렬이다.
- [ ] 행 hover 시 배경 변화가 자연스럽고 깨짐이 없다.

### 스크린샷
- [ ] 전체 화면
- [ ] 첨부 컬럼 확대

## 2) `/master/performance-plan` 성과 계획 관리
### 체크 항목
- [ ] 우상단 액션 버튼(`전년 복사/엑셀 양식 다운로드/저장`) 기준선이 맞다.
- [ ] 조회 필터 영역과 상단 액션 영역 사이 간격이 일정하다.
- [ ] 테이블 텍스트가 세로로 꺾이지 않고 가로로 보인다.
- [ ] 월 입력칸 숫자 4~5자리가 잘리지 않는다.
- [ ] 가로 스크롤 시 헤더/본문 경계가 어긋나지 않는다.
- [ ] 상세 모달 오픈 시 본문/이력 목록 레이아웃이 무너지지 않는다.

### 스크린샷
- [ ] 전체 화면
- [ ] 테이블 + 우상단 버튼 영역 확대

## 3) `/partner/notice` 공지/자료실
### 체크 항목
- [ ] 탭/필터/테이블 카드 스타일이 프로젝트 공통 톤과 맞다.
- [ ] 긴 제목 표시 시 줄바꿈/툴팁/행 높이가 자연스럽다.
- [ ] 상세 모달에서 긴 본문 스크롤이 자연스럽다.
- [ ] 첨부파일 목록 링크 간격/정렬이 깨지지 않는다.
- [ ] 등록 모달 폼(유형/기간/내용/첨부) 간격이 균일하다.
- [ ] 모달 닫기/배경 클릭 시 의도대로 닫힌다.

### 스크린샷
- [ ] 목록 화면
- [ ] 상세 모달(긴 본문)

## 결과 기록
| 페이지 | 1366x768 | 1920x1080 | 비고 |
|---|---|---|---|
| `/sales/material` |  |  |  |
| `/master/performance-plan` |  |  |  |
| `/partner/notice` |  |  |  |

## 실패 시 바로 수정 우선순위
1. 텍스트 세로/깨짐, 버튼 잘림
2. 버튼/입력 정렬 불일치
3. 모달 스크롤/오버레이 동작 이상


